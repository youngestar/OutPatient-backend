server:
  port: 8203
spring:
  application:
    name: outpatient-backend-auth
  profiles:
    active: dev
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  session:
    timeout: 2592000

  # 数据库相关配置
  datasource:
    url: jdbc:mysql://localhost:3306/hospital_management_system
    username: root
    password: 258013
    hikari:
      max-lifetime: 1800000
      idle-timeout: 600000
      minimum-idle: 5

  mail:
    host: smtp.qq.com
    default-encoding: UTF-8
    username: ${MAIL_USERNAME:235857689@qq.com}
    password: ${MAIL_PASSWORD:cxrcsizayqlkbhdf} # 使用环境变量提供 QQ 邮箱授权码
    port: 465
    protocol: smtps # 改为smtps协议
    test-connection: false # 禁用启动时测试连接
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          socketFactory:
            class: javax.net.ssl.SSLSocketFactory
            fallback: false
          connectiontimeout: 10000
          timeout: 10000
          writetimeout: 10000

  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      lettuce:
        pool:
          max-active: 10
          max-idle: 10
          min-idle: 1
          time-between-eviction-runs: 10s
      timeout: 5000
      password: 258013

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# MinIO 配置
minio:
  endpoint: http://localhost:9000 # MinIO 服务器地址
  access-key: minioadmin # 访问密钥
  secret-key: minioadmin # 秘密密钥
  bucket-name: user-avatar # 存储桶名称

  # RabbitMQ 配置
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /
    # 连接超时配置
    connection-timeout: 5000
    # 模板配置
    template:
      retry:
        enabled: true
        initial-interval: 1000ms
        max-attempts: 3
        multiplier: 1.0
        max-interval: 10000ms

# Sa-Token配置（Gateway专用）
sa-token:
  # taken 名称
  token-name: satoken
  # token 风格
  token-style: uuid
  # token 有效期
  timeout: 2592000
  # token 最低活跃频率
  active-timeout: -1
  # token 持久化
  is-save-session: true
  # 是否允许同一账号多地同时登录
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个 token
  is-share: true
  # 是否输出 Sa-Token 日志
  is-log: true
  # Jwt 签名密钥
  jwt-secret-key: graduation-gateway-secret-key-2025
  auto-renew: true

  # Redis配置（如果使用Redis存储token）
  token-prefix: "satoken:"
  is-read-body: false
  is-read-header: true
  is-read-cookie: false
  dynamic-active-timeout: true

# Auth模块配置
auth:
  global:
    token-expire: 2592000
    verify-code-expire: 5
    verify-code-length: 6
    max-login-attempts: 5
    login-lock-time: 30
    password-min-length: 6
    password-max-length: 20
    session-timeout: 30
    allow-multi-login: true
    avatar-allowed-types: ["jpg", "jpeg", "png", "gif"]
    avatar-max-size: 2048

ai-service:
  api-url: https://api.deepseek.com
  api-key: ${AI_API_KEY:sk-7c36ac350dcf4f498f4141e83feb9bd2}